@model IEnumerable<TrabalhoPT.Models.CardsModel>

@{
    ViewBag.Title = "GetCardsByAjaxRequest";
}

<ul id="lists" class="connectedSortable">
@foreach (var card in @Model)
{
    var link = "/Cards/GetCard/" + @card.Id;
    <li id="@card.Id" class="ui-sortable"><a href=@link>@card.Name</a></li>
}
@*@if(Model.Count() != 0){
    @Html.ActionLink("Adicionar Cartão", "CreateCard", "Lists", new { id = Model.First().List.Id}, null)
}*@
</ul>
